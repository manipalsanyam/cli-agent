# Core System Dependencies 
requests>=2.31.0          # Used by chat_service and model_service for API calls
python-dotenv>=1.0.0      # Used in main.py for environment configuration

# Service: UI & Console 
# (Uses standard library 'os' and 'sys', no extra pip packages needed)

# Development & Testing 
# (Recommended for the monorepo workflow)
pytest>=7.0.0             # To run unit tests against individual services
